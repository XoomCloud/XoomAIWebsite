const $=(s)=>document.querySelector(s);const $$=(s)=>[...document.querySelectorAll(s)];
const menu=$('.menu-toggle');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));$('#navigation').classList.toggle('open',open)});$$('nav a').forEach(a=>a.addEventListener('click',()=>{menu?.setAttribute('aria-expanded','false');$('#navigation')?.classList.remove('open')}));
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu?.getAttribute('aria-expanded')==='true'){menu.click();menu.focus()}});
const tiers={assist:{base:1250,hours:8,staff:50,increment:.5},operations:{base:2500,hours:11,staff:100,increment:.75},partner:{base:4500,hours:13,staff:150,increment:1}};
function calculate(count){const n=Math.max(1,Math.min(100,Math.round(Number(count)||10)));const extra=Math.max(n-10,0);Object.entries(tiers).forEach(([key,tier])=>{$(`[data-price="${key}"]`).textContent=new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(tier.base+extra*tier.staff);$(`[data-hours="${key}"]`).textContent=tier.hours+extra*tier.increment});$('#staff').value=n;$('#staff-range').value=n;$('#contact-staff').value=n;$('#capacity-note').hidden=n<=50}
$('#staff-range')?.addEventListener('input',e=>calculate(e.target.value));$('#staff')?.addEventListener('change',e=>calculate(e.target.value));$$('[data-tier]').forEach(a=>a.addEventListener('click',()=>{$('#interest').value=a.dataset.tier}));
const examples={enquiries:['AI OPERATIONS / ILLUSTRATIVE WORKFLOW','An enquiry arrives.<br>The right work follows.','Capture the enquiry, prepare a response and update the CRM. Your team checks the important decisions before anything goes out.','New client enquiry','Prepare response + CRM record','Review. Approve. Send.'],knowledge:['AI ASSIST / ILLUSTRATIVE WORKFLOW','Your knowledge.<br>Ready when it matters.','Organise approved source material and refine your assistant. Help staff find useful answers while keeping permissions and source ownership clear.','Approved team knowledge','Configure assistant + test answers','Check sources. Apply judgment.'],reporting:['AI OPERATIONS / ILLUSTRATIVE WORKFLOW','Reporting that starts<br>ahead of the meeting.','Collect approved inputs and prepare a reporting draft. Your team validates the figures and commentary before sharing the report.','Approved business data','Compile reporting draft','Validate. Approve. Share.']};
function chooseTab(button){$$('[data-work]').forEach(b=>{b.setAttribute('aria-selected',String(b===button));b.tabIndex=b===button?0:-1});const x=examples[button.dataset.work];$('#workflow-panel').setAttribute('aria-labelledby',button.id);$('#work-label').textContent=x[0];$('#work-title').innerHTML=x[1];$('#work-copy').textContent=x[2];['node1','node2','node3'].forEach((id,i)=>$('#'+id).textContent=x[i+3]);$('.workflow-visual').setAttribute('aria-label',x.slice(3).join(', then '))}
$$('[data-work]').forEach(b=>{b.addEventListener('click',()=>chooseTab(b));b.addEventListener('keydown',e=>{if(!['ArrowRight','ArrowLeft','Home','End'].includes(e.key))return;e.preventDefault();const tabs=$$('[data-work]');let i=tabs.indexOf(b);i=e.key==='Home'?0:e.key==='End'?tabs.length-1:(i+(e.key==='ArrowRight'?1:-1)+tabs.length)%tabs.length;chooseTab(tabs[i]);tabs[i].focus()})});
$('#watch-film')?.addEventListener('click',()=>{$('#film-dialog').showModal();$('#brand-film').play().catch(()=>{})});$('.dialog-close')?.addEventListener('click',()=>$('#film-dialog').close());$('#film-dialog')?.addEventListener('close',()=>{$('#brand-film').pause();$('#watch-film').focus()});
$$('[data-booking]').forEach(a=>a.href=window.XOOM_CONFIG.bookingUrl);$$('[data-email]').forEach(a=>{a.href='mailto:'+window.XOOM_CONFIG.email;a.textContent=window.XOOM_CONFIG.email+' ↗'});
$('#enquiry-form')?.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(e.currentTarget);const subject='XoomTalent enquiry — '+data.get('company');const message=`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nCompany: ${data.get('company')}\nInterest: ${data.get('interest')}\nCovered staff: ${data.get('staff')}\n\nWhat we want to improve:\n${data.get('message')}\n\nPlease contact me about this enquiry.`;$('#email-preview').value=subject+'\n\n'+message;$('#form-result').hidden=false;window.location.href=`mailto:${window.XOOM_CONFIG.email}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(message)}`});
$('#copy-enquiry')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText($('#email-preview').value);$('#copy-status').textContent='Copied.'}catch{$('#email-preview').focus();$('#email-preview').select();$('#copy-status').textContent='Select and copy the highlighted text.'}});
if(location.hash==='#partner-enquiry'&&$('#interest')){$('#interest').value='A recruiter partnership';$('#contact').scrollIntoView()}
